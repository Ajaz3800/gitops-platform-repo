apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: k8s-security
  namespace: argocd

spec:
  description: Platform components (security, networking, observability)

  sourceRepos:
    - 'git@github.com:Ajaz3800/gitops-platform-repo.git'

  destinations:
    - namespace: 'k8s-security'
      server: https://kubernetes.default.svc
